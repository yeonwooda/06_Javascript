// str, number, boolean, array, obj,

// "". ~~~()
// 문자열을 바꿔주는 기능들
// 문자열의 함수들

// f(x) -> slit("i"), toUpperCase(), , ~~~()
// f(x) = x+100
// 100 -> 200
// 200 -> 300
// 300 -> 400
// 1000 -> 1100

const a = "americano".toUpperCase(); // 대문자
console.log(a);

const b = "americano".split("i"); // 자르기
console.log(b);

const c = "americano".includes("a"); // 포함
console.log(c);
